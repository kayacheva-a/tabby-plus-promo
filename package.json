{
  "name": "tabby-csv-updater",
  "version": "1.0.0",
  "description": "Cloudflare Worker for updating GitHub CSV files",
  "main": "api/update-csv.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
